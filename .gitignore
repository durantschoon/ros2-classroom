.#*
*~

# Local configuration; .env.example is the committed template.
.env

# Optional compose overrides documented in docs/hardware-and-gpu.md
compose.gpu.yaml
compose.bind.yaml
workspace-src/

# Agent worktrees are created inside the repo; never commit them.
.claude/worktrees/

# Python bytecode, e.g. from `make check`.
__pycache__/
